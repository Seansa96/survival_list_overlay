<Window x:Class="survival_list_overlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:survival_list_overlay"
        mc:Ignorable="d"
        Title="SG Overlay" Height="200" Width="300"
        MouseDown="Window_MouseDown"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="True">

    <!-- Outer container that tracks mouse enter/leave -->
    <Grid x:Name="RootGrid"
          MouseEnter="RootGrid_MouseEnter"
          MouseLeave="RootGrid_MouseLeave">

        <!-- Title Bar (hidden by default) -->
        <Border x:Name="TitleBar"
                Background="#66000000"
                Height="30"
                VerticalAlignment="Top"
                CornerRadius="8,8,0,0"
                Visibility="Collapsed"
                >
            <DockPanel>
                <TextBlock Text="SG Overlay"
                           Foreground="White"
                           VerticalAlignment="Center"
                           Margin="10,0" />
                <Button Content="X"
                        DockPanel.Dock="Right"
                        Click="Close_Click"
                        Width="30"
                        Height="30"
                        Padding="0" />
            </DockPanel>
        </Border>

        <!-- Main overlay UI -->
        <Border Background="#AA111111" CornerRadius="12" Padding="10" Margin="0,30,0,0">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding Items}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Name}" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                
                                <!-- Progress Bar -->
                                <Grid Grid.Column="1" Height="25">
                                    <ProgressBar Value="{Binding Progress}" Maximum="{Binding Total}" Height="15"/>
                                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}{0} / {1}">
                                                <Binding Path="Progress" />
                                                <Binding Path="Total" />
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                    
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="10,0,0,0" VerticalAlignment="Center">
                                        <Button Content="-" Width="20" Height="20" Command="{Binding DecrementCommand}" />
                                        <Button Content="+" Width="20" Height="20" Command="{Binding IncrementCommand}" Margin="5,0,0,0"/>
                                    </StackPanel>
                                    
                                </Grid>
                                
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </Grid>
</Window>
